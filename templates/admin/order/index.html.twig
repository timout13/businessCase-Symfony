{% extends 'admin_base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center flex-wrap mb-5">
        <h1 class="">Administration des commandes</h1>
        <a href="{{ path('admin_category_new') }}" class="btn btn-success">Ajouter une commande</a>
    </div>
    <table class="table mt-4">
        <thead>
        <tr>
            <th>Id</th>
            <th>Date de la commande</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody class="align-middle">
        {% for order in orders %}
            <tr>
                <td>{{ order.id }}</td>
                <td>{{ order.date_order }}</td>
                <td>
                    <a href="{{ path('admin_category_edit', {'id': order.id}) }}" class="btn btn-success">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucune commande trouvée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
