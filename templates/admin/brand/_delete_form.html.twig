<form method="post" action="{{ path('admin_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette marque ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
